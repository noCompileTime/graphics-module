#=======================================================================================================================
target_include_directories(${PROJECT_NAME} PUBLIC .)     # TODO remove this CMAKE file? seems to not be needed
target_precompile_headers(${PROJECT_NAME}  PUBLIC headers.hpp)
#=======================================================================================================================

#=======================================================================================================================
add_subdirectory(opengl)
#=======================================================================================================================